/**
 * Shared by every docs instance — each one autogenerates its sidebar from
 * its own content directory, so adding a server means adding a folder,
 * not editing this file. Order within a section comes from the
 * `sidebar_position` frontmatter on each page.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  main: [{ type: 'autogenerated', dirName: '.' }],
};

export default sidebars;
