# Comma-separated bearer tokens; issue one per person/app, revoke by removing.
FROOTS_TOKENS=froots_example_token_change_me

# Provider keys (server-side — clients never see these)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional model overrides
# GEMINI_IMAGE_MODEL=gemini-3.1-flash-image
# OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst-2026-09-08
# GEMINI_TEXT_MODEL=gemini-2.5-flash
# OPENAI_TEXT_MODEL=gpt-4.1-mini
